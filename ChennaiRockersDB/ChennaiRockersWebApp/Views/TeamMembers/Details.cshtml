@model ChennaiRockersWebApp.Models.TeamMember

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>TeamMember</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MemberName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MemberName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.MemberEmail)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MemberEmail)
        </dd>

    </dl>
    <table class="table">
        <tr>
            <th>
                Date
            </th>
            <th>
                Venue
            </th>
            <th>
                Amount
            </th>
        </tr>
        @{ int total = 0;
            foreach (var item in Model.Games)
            {
                total = total + Convert.ToInt32(item.courtFees);
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.GameDateTime)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Venue)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.courtFees)
                    </td>
                </tr>
            }
            <tr>
                <td>
                    Total Amount : @Html.DisplayFor(a => total)
                </td>
            </tr>
        }
    </table>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.MemberId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
